.env
.env.local
__pycache__/
*.py[cod]
.pytest_cache/
output*/
*.egg-info/
*.log
*_log.txt

# Vendored clones used only for one-off experiments (do not commit)
yt-dlp-repo/

# Local environments
.venv/
venv/
.DS_Store

# Keep the repo lean: only the core engine is tracked.
# Personal testing helpers, sample projects, run outputs, and scratch dirs are local-only.
scripts/
tests/
run_filelock_now.sh
test_projects/
manual_testing/
docs/
external/
runs/
smoke_onefunc/
logs/
configs/

# Documentation: README.md is the only markdown tracked in the repo.
*.md
!README.md
